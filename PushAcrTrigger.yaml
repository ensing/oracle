resources:
  containers:
  - container: nginx
    type: ACR
    azureSubscription: MY-Azure-connection
    resourceGroup: test-trigger
    registry: acrtriggertest
    repository: nginx
    trigger:
     enabled: true
